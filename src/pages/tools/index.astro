---
import ToolsIndexContent from '@/components/pages/ToolsIndexContent.astro';
import type { Locale } from '@/lib/i18n/translations';

const locale = (Astro.currentLocale as Locale) || 'es';
---

<ToolsIndexContent {locale} />
